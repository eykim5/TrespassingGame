/// @description Initializes grid and items

init_inventory();
create_inventory();

// See function. Inserts item into first available empty slot or adds to matching slot.
	//inventory_insert_item(item.p12, 1);
	//inventory_insert_item(item.p12, 2);

// Designates matching slot for specific item.
	//global.inventory[# 3, 0] = item.p12;
// Modify above slot using this.
	//inventory_modify_slot_amount(3, 2, true);